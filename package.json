{
  "name": "nta-exam-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "cd nta-backend && npm install && npx prisma generate && cd ../nta-exam && npm install && npm run build",
    "start": "cd nta-backend && npx prisma db push && node src/index.js"
  }
}
