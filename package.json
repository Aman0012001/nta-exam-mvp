{
  "name": "nta-exam-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "cd nta-backend && npm install && npx -y cross-env DATABASE_URL=postgresql://dummy npx prisma generate && cd ../nta-exam && npm install && npm run build",
    "start": "cd nta-backend && npx prisma db push && node prisma/seed.js && node src/index.js"
  }
}
